Shveiny Market KZ — shv10 (инструкция подробная)

Структура проекта:
- index.html
- categories/
   - categories.html
   - nitki.html
   - zamki.html
   - pugovitsy.html
   - furnitura.html
   - prochee.html
- css/style.css
- js/script.js
- js/data.json
- js/config.json
- images/
- README_RU.txt

Запуск локально:
1) Открыть через локальный сервер (рекомендуется):
   python -m http.server 8000
   Перейти в http://localhost:8000
2) Или открыть index.html напрямую (некоторые браузеры блокируют fetch для локальных json).

Как работают данные:
- Товары и категории загружаются из js/data.json.
- Соцсети, адрес и часы — в js/config.json.
- Корзина хранится в localStorage под ключом "shv_cart_v10".

Как изменить единицу измерения товара:
1) Откройте js/data.json.
2) Каждый товар содержит поле "unit": "шт" или "м".
   - Пример:
     {
       "id":"zamok-roll",
       "title":"Замок рулонный (по метру)",
       "price":250,
       "unit":"м",
       "step":1,
       "min":1,
       "image":"../images/zamok_roll.jpg"
     }
3) Если unit="м" и step=1 и min=1 — дробные значения запрещены (продаётся целыми метрами).
4) Если unit="шт" — товар считается целым.

Как добавить/редактировать товар:
1) Поместите изображение в папку /images/, например images/new_item.jpg.
2) Откройте js/data.json и найдите нужную категорию (ключ).
3) Добавьте объект в массив, пример:
   {
     "id":"novyy-1",
     "title":"Новый товар",
     "price":1200,
     "unit":"шт",
     "image":"../images/new_item.jpg"
   }
4) Сохраните файл. При перезагрузке страницы товар появится в каталоге.

Пояснения по полям товара:
- id — уникальный идентификатор (строка).
- title — название товара (строка).
- price — цена за единицу (число).
- unit — "шт" или "м".
- image — путь к картинке (относительный к файлу категории, используем ../images/...).
- step — шаг для input (используется для метровых товаров, например 1).
- min — минимальное значение (например 1).

Как добавить новую категорию:
1) Создайте файл categories/newcat.html. Можно скопировать структуру из других страниц.
2) Добавьте ссылку/карточку в categories/categories.html:
   <a class="cat-card" href="newcat.html">
     <img src="../images/newcat.jpg" alt="">
     <h3>Новая категория</h3>
     <p>Описание категории</p>
   </a>
3) В js/data.json добавьте ключ "newcat": [ ...товары... ].
4) Сохраните и перезагрузите сайт.

Как изменить соцсети, адрес и часы работы:
- Файл: js/config.json
  {
    "socials": { "whatsapp":"https://wa.me/...", "instagram":"https://instagram.com/...", "tiktok":"...", "2gis":"..." },
    "address":"Рынок Алаш · Тамерлановское шоссе, 198/34",
    "hours":"пн–вс: 09:00–18:00 (без выходных)"
  }

Как работать с изображениями:
- Все изображения лежат в папке /images/.
- Используйте относительный путь в поле image, например "../images/name.jpg" в категориях.
- Для логотипа замените images/logo.svg на свой файл (svg или png).

Как изменить стиль (цвета, шрифты):
- Файл: css/style.css
- В начале файла задаются переменные :root. Измените --accent, --accent2 и т.д.

Как корзина считает сумму:
- Мини-корзина показывает цену за единицу и цену позиции = price * qty.
- Итоговая сумма считается как сумма цен всех позиций и отображается внизу.

Как оформить заказ:
- Нажмите "📦 Оформить заказ" в мини-корзине — сформируется WhatsApp-ссылка с текстом заказа (содержит товар/количество/цвет/итого) и откроется мессенджер.

Полезные советы:
- Если при открытии через file:// вам не загружаются JSON, используйте локальный сервер (python -m http.server).
- Убедитесь, что пути к картинкам корректные (вложенность ../).
- Для редактирования больших данных используйте JSON-редактор, чтобы не допустить синтаксической ошибки.

Если нужно — могу:
- Пересобрать архив в zip и дать ссылку,
- Сразу добавить несколько реальных изображений,
- Подготовить 1-2 примера новой категории и товара.

